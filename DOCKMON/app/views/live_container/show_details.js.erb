$("#container_modal").html("<%= escape_javascript(render 'show_details')%>")
$("#container_modal").modal({
		'backdrop': false,
		'show': true,
		'keyboard': false,
		'top': '0px',
		'width': 'auto'
	});